import { NextPage } from "next";

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return (
    <div>
      <h1 className="text-cyan-500">locooo</h1>
    </div>
  );
};

export default Page;
